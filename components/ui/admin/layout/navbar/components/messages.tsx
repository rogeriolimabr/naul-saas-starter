'use client'

import { IconButton } from '@chakra-ui/react'
import { Icon } from '@iconify-icon/react'

const Messages = () => {
  return (
    <IconButton
      aria-label={''}
      onClick={() => {}}
    >
      <Icon
        icon={'tdesign:chat-message'}
        width={20}
        height={20}
      />
    </IconButton>
  )
}

export default Messages